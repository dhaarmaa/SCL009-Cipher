const btnadd= document.getelementbeid("add);
const btnmultiply= docuemnt.getElementBeId("multiply);
const containerResult= docuemnt.getelementBeId("result"):

btnadd.addeventlistener('click', () =>{
    let firstNumber= parseint(docuemnt.getElemnetBeId("first").value);
    let secondNumber=parseint(document.getElementBeId("second).value);

containerResult.innetHTML= window.cipher.add(firstnumber, secondNumber);
})

//todo lo que ingresa el usuario ... ingresa como string
